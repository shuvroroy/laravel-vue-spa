const state = {};

const getters = {};

const actions = {
  register({ form }) {
    console.log(form);
  },
};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
